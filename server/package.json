{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "db:migrate-json": "ts-node-dev --transpile-only src/migrateJsonToPostgres.ts",
    "seed": "ts-node-dev --transpile-only src/seed.ts",
    "test": "npm run build && mkdir -p dist/src/resumeParse && cp -R src/resumeParse/__fixtures__ dist/src/resumeParse/ && cp -R src/resumeParse/__snapshots__ dist/src/resumeParse/ && node --test dist/src/resumeParse/parseResumePdf.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.16.0",
    "@supabase/supabase-js": "^2.98.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "openai": "^6.24.0",
    "pdfjs-dist": "^4.10.38",
    "prisma": "^6.16.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "minimatch": "3.1.4"
  }
}
